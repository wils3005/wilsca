enum DBNames {
  DB1 = "db1",
}

enum LogLevel {
  TRACE = "trace",
  DEBUG = "debug",
  INFO = "info",
  WARN = "warn",
  ERROR = "error",
}

enum ObjectStoreNames {}

export { DBNames, LogLevel, ObjectStoreNames };
