// import UUID from "uuid";
// import VideoElement from "./video-component";

// class VideoSection {
//   elements = new Map<string, VideoElement>();

//   constructor() {
//     document
//       .querySelectorAll<HTMLVideoElement>("section#video > video")
//       .forEach((e) => this.elements.set(UUID.v4(), new VideoElement(e)));
//   }

//   addMediaStream(mediaStream: MediaStream): void {
//     new VideoElement().setMediaStream(mediaStream);
//   }
// }

// export default VideoSection;
