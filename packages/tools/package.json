{
  "name": "@wilsjs/tools",
  "scripts": {
    "build": "npx tsc --project tsconfig.build.json",
    "postbuild": "chmod +x build/*",
    "test": "echo \"Error: run tests from root\" && exit 1"
  }
}
